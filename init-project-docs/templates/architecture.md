# Architecture — L2 架构层

> 本文档定位:让读者在脑里画出"项目骨架图"。不涉及具体实现细节。
> 三段式:[是什么] / [为什么] / [不变量]

---

## [是什么] — 模块清单

<!-- 与用户对话后填:列出主要模块(5-10 个,过多说明拆分粒度不对)

| 模块 | 定位 | 设计哲学 | 作用 |
|---|---|---|---|
| ... | ... | ... | ... |
-->

---

## [是什么] — 模块间关系

<!-- 与用户对话后填:数据流方向、依赖关系、调用链
- 可以用文字描述
- 也可以用 ASCII 图 / mermaid / draw.io 图
- 重点是"谁依赖谁、数据怎么流"
-->

---

## [是什么] — 主流程叙述(最重要的部分)

<!-- 与用户对话后填:10-20 行,从入口到出口的关键路径
这是 L2 最关键的部分。让 AI 在不读代码的情况下,理解"故事怎么开始、怎么结束"。
后续做任何模块工作前,先读这段建立全局感。
-->

```
用户操作 → 入口函数 → 模块A 处理 → 模块B 持久化 → ... → 出口
```

---

## [为什么] — 架构决策

<!-- 与用户对话后填:
- 为什么这么拆(粒度选择的理由)
- 考虑过的替代架构(被拒方案是金矿)
- 关键架构决策 → 引用 docs/decisions/adr-XXX-*.md
-->

---

## [不变量] — 架构级约束

<!-- 与用户对话后填:
- 依赖方向(单向?无环?)
- 数据流方向
- 模块边界规则(什么不能跨模块做)
- 跨模块通信方式(直接调用?事件?消息?)
- 并发模型假设
-->
