"""check 命令组已移除 — 改为对应 read-*/list-* 命令的 --verify 参数。

参见：
  read-structure --verify   → 样式异常标注（原 check-headings）
  read-section --deep --verify → 正文格式检查（原 check-body）
  read-page-setup --verify  → 页面设置验证（原 check-page-setup）
  list-references --verify  → 引用一致性验证（原 check-references）
  search --writing-style    → 写作风格搜索（原 check-style）
"""
